# Databricks notebook source
# MAGIC %md
# MAGIC 1. Carregue um arquivo CSV em um dataframe pandas e imprima as primeiras 5 linhas.
# MAGIC 2. Selecione uma coluna específica do dataframe e calcule a média dos valores nessa coluna.
# MAGIC 3. Adicione uma nova coluna ao dataframe com uma operação matemática envolvendo outras colunas.
# MAGIC 4. Filtre o dataframe para selecionar apenas linhas com valores específicos em uma coluna.
# MAGIC 5. Agrupe os dados por uma coluna e calcule a soma de outra coluna para cada grupo.
# MAGIC 6. Substitua valores ausentes ou faltantes em uma coluna com a média dos valores nessa coluna.
# MAGIC 7. Concatene dois dataframes em um único dataframe.
# MAGIC 8. Exporte o dataframe para um arquivo CSV.
