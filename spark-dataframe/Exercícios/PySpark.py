# Databricks notebook source
# MAGIC %md
# MAGIC 1. Crie um RDD com a lista de números inteiros de 1 a 100 e aplique uma função de map para retornar o quadrado de cada elemento.
# MAGIC 2. Carregue um arquivo CSV e filtre todas as linhas que possuem valor "NULL" na coluna "NOME".
# MAGIC 3. Crie uma função de agregação para calcular a média dos valores de uma coluna em um DataFrame.
# MAGIC 4. Dado dois RDDs, crie uma união dos dois RDDs mantendo apenas os elementos únicos.
# MAGIC 5. Carregue um arquivo JSON e agrupe os dados por uma coluna específica, contando o número de ocorrências de cada grupo.
# MAGIC 6. Carregue um arquivo texto e conte a ocorrência de cada palavra no arquivo.
# MAGIC 7. Aplique uma transformação de reduceByKey para calcular a soma dos valores de uma coluna em um RDD com chave-valor.
# MAGIC 8. Crie uma função para remover linhas duplicadas em um DataFrame.
# MAGIC 9. Carregue uma tabela do Spark SQL e aplique uma operação de join com outra tabela carregada do banco de dados.
# MAGIC 10. Crie uma aplicação de processamento de lote para calcular estatísticas básicas (média, desvio padrão, etc.) em um conjunto de dados.
